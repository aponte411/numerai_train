---
version: 1

kind: experiment

framework: xgboost

tags: [train_models]

environment:
  resources:
    gpu:
      requests: 1
      limits: 1

build:
  image: gitlab.beno.ai:4567/ai/hypgen/optimus-prime/gpu2:baiflow
  build_steps:
    - pip install -r requirements.txt

run:
  cmd: python -u /predict.py --model xgboost
